#include <stdio.h>
#include <string.h>
#include "copy.h"
char line[MAXLINE];
char longest[MAXLINE];

int main() {
	int len, max, count = 0;
	line
	for (i = 0; i < 5; i++) {
		fgets(line, MAXLINE, stdin);
		len = strlen(line);	
	}
	char *p
	printf("%s", longest);

	return 0;
}
